Create a button inside the header that toggles a dark theme.

Add JS in script.js to listen for clicks and flip a data-theme attribute on <body>.

Update styles.css to style [data-theme="dark"].

Polish: lint HTML, minify CSS.